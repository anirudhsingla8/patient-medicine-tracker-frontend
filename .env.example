# Frontend environment variables for Medicine Tracker (copy to .env.local or .env)
# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080

# Optional: If deploying behind a different origin, set to your backend URL
# VITE_API_BASE_URL=https://api.my-domain.com

# Optional: Application display name
# VITE_APP_NAME=Medicine Tracker
